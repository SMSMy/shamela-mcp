# UPSTREAM.md — العلاقة بالمنبع (alhoqbani/shamela-mcp)

هذا المستودع **امتداد مُعلن** لـ [alhoqbani/shamela-mcp](https://github.com/alhoqbani/shamela-mcp) (ترخيص MIT)، لا منافس له. هذه الوثيقة بديل رابط «synced from» الذي لا يظهر لأن المستودع **ليس fork مسجلاً على GitHub** بل نسخة مستقلة.

## نقطة المزامنة

- **المحتوى مطابق للمنبع حتى الدمج `b674d0e`** (PR #54، أغسطس 2026)، ويشمل:
  - كل إصلاحات v1.3.0: البحث داخل المحرك، جدول الهمزات، علاج الصفر المضلل، دقة تغطية الكتب، تحسينات العزو.
  - ما بعده: إصلاح ملاحظات الإصدار العربية (#51)، مراجعة المطابقة البروتوكولية (#52)، تحويل السطح إلى مكتبة (#53)، تعدد لغات صفحة الهبوط (#54).
- آخر فحص مزامنة: 2026-08-31.

## لم يُنقل من المنبع — ولماذا

| التزامات المنبع | السبب |
| --- | --- |
| #55، #56، #57 — الإشارة إلى `shamela.link` وشريط «الخادم البعيد» في صفحة الهبوط | توثيقات تروّج لخادم المنبع البعيد وموقعه؛ هذه الشوكة لا تشغّله، وروابطها تبقى على `shamela.ws` |

## إضافاتنا على المنبع

- **أداة `shamela_skill`** (رقم 35) ومهارة الباحث مضمّنة في الحزمة (v1.4.0، وتصحيح نصّها v1.4.1).
- توثيق أجنت محمول لكل البيئات: `AGENTS.md` + تعميم `CLAUDE.md` + README متعدد المضيفات.
- إصلاح اقتباس الوسائط على ويندوز في `pack.mjs`/`release.mjs` (مسارات فيها مسافات).
- قوالب Issues + اختبار «المهارة لا تسمّي إلا أدوات حقيقية» + بصمة SHA-256 في كل إصدار.

## روتين المزامنة (عند كل إصدار أو دورياً)

```bash
git fetch upstream main
git log --oneline main..upstream/main    # ما فاتنا من المنبع
git log --oneline upstream/main..main    # إضافاتنا نحن
```

- انقل **إصلاحات منطق البحث** فوراً (أولوية مطلقة)، وقيّم التوثيقات كلٌّ على حدة (بعضها يخص بنية المنبع ولا يناسبنا — يُوثَّق أعلاه).
- بعد كل مزامنة: حدّث جدول «نقطة المزامنة» في هذا الملف.

## ماذا يمكن أن يعود إلى المنبع

- إصلاح اقتباس الوسائط على ويندوز — عام، يصلح للمنبع كما هو (مرشح للرفع كـ PR).
- `shamela_skill` ومهارة الباحث — إن رغب المنبع في تبنيهما.
