# AGENTS.md — تعليمات إلزامية للأيجنت الذي يستخدم هذا الـ MCP

> هذا ملف موجّه **للأيجنت (AI agent)** الذي يحمّل سيرفر shamela MCP — اقرأه كاملاً قبل أول استخدام.
> المستخدم البشري لا يحتاج قراءته.

## ما هذا السيرفر؟

مكتبة الشاملة 4 (8,598 كتاباً، 3,190 مؤلفاً، 41 تصنيفاً) كسيرفر MCP للبحث والاستشهاد.
السيرفر **استرجاع فقط** — لا يكتب، لا يحذف، لا يتصل بالإنترنت. كل أدواته تبدأ بـ `shamela_` (34 أداة).

## ⚠️ القاعدة الأولى: لا تخترع شيئاً — استخدم الأدوات مباشرة

- الأدوات الأصلية (`shamela_search_pages`, `shamela_search_phrase`, `shamela_get_page`, `shamela_get_citation`...) **متاحة لك كأدوات MCP مباشرة** — استدعِها كما تستدعي أي أداة أخرى.
- **ممنوع نهائياً**: كتابة سكربتات يدوية، عملاء MCP مخصصين، عمليات خلفية، polling، أو محاكاة البروتوكول يدوياً. هذا أحرق 11 مليون توكن في جلسة سابقة ووصلنا لصفر نتيجة — لا تكرره.
- الأداة الرسمية تجيب خلال **ثوانٍ**. إذا شعرت أنك "تعيد اختراع العجلة" → توقف فوراً وارجع للأدوات الأصلية.

## 🔧 التثبيت — لا تفترض المسار أبداً

السيرفر يحتاج متغيرين بيئيين. **المسار يختلف من جهاز لآخر — اسأل المستخدم ولا تفترض:**

```bash
openclaw mcp set shamela '{
  "command": "node",
  "args": ["<مسار>/dist/index.js"],
  "env": {
    "SHAMELA_INSTALL_ROOT": "<مسار تثبيت الشاملة>",  # ⚠️ قد يكون D:\shamela4 أو غيره
    "SHAMELA_JRE": "<مسار java>"
  },
  "cwd": "<مسار المشروع>"
}'
```

- **`SHAMELA_INSTALL_ROOT`**: المجلد الذي يحتوي `database/` و `app/` — على ويندوز عادة `D:\shamela4`، على ماك `~/Library/Application Support/Shamela`، وقد يكون أي مكان آخر. **اسأل المستخدم عن المسار الفعلي** إذا لم يكن واضحاً — لا تبحث عنه بنفسك وتحرق وقتاً.
- **`SHAMELA_JRE`**: على ويندوز مع WSL2 يستخدم `/usr/bin/java` (OpenJDK 21) — وإلا فسيرفر الـ JRE المرفق.

## 🕐 الإحماء والمهلات (سبب فشل سابق)

- السيرفر يحتاج **10-15 ثانية** لإقلاع JVM + تحميل فهارس Lucene (7.6 مليون صفحة).
- إذا فشل الـ probe بـ "Request timed out" → **زد المهلة** بدل أن تظن السيرفر معطلاً:
  ```bash
  openclaw mcp configure shamela --connect-timeout 60 --timeout 300
  openclaw mcp reload
  ```
- بعد تغيير إعدادات MCP أو إعادة تشغيل OpenClaw: الأدوات تظهر في **الجلسات الجديدة** فقط — إذا لم تجد أدوات `shamela_*` في جلسة حالية، اطلب من المستخدم فتح جلسة جديدة. **لا تلجأ لسكربتات بديلة.**

## 🔍 طريقة البحث الصحيحة (دروس من أخطاء سابقة)

1. **ابدأ بالعبارة الدقيقة**: `shamela_search_phrase(query, mode="phrase")` — يبحث عن تتابع كلمات متتالية.
2. **الرواية تختلف**: نفس البيت/المقولة تأتي بألفاظ مختلفة (ربي/ربّ، قدّر/قدر، همزات، تشكيل). إذا أعطى البحث الحرفي صفراً → **جرّب صيغاً أوسع** (بدون كلمات زائدة، أو الجزء المميز فقط).
3. **التمييز بين النتائج**: قد تجمع كلمة بحث نتائج لا علاقة لها بالمراد (استعمالات لغوية، جمل أخرى) — افحص الـ snippets وعدّ الكتب ذات الصلة فعلاً.
4. **لا تخلط المصادر**: كل نتيجة من الشاملة = إحالة موثقة عبر `shamela_get_citation`. لا تنسب نصاً لمصدر لم تقرأه.

## 💬 متى تسأل المستخدم (بدل حرق التوكنات)

اسأل المستخدم **قبل** أن تبدأ عملاً مكلفاً إذا كان غير واضح:
- مسار تثبيت الشاملة غير معروف → اسأل، لا تبحث.
- العبارة/الموضوع غامض أو قد يكون محرّفاً → اذكر الاختلافات المحتملة واسأل أيها يقصد.
- الكتاب موجود في الفهرس لكنه غير منزّل (`downloaded=false`) → أخبر المستخدم أنه يحتاج تنزيله من تطبيق الشاملة، ولا تحاول القراءة.
- النتائج كثيرة جداً (>10) → اعرض أبرزها واسأل عن الباقي.

**المبدأ:** البحث الواحد عبر الأدوات الأصلية رخيص وسريع — لكن البحث العشوائي المتكرر بدون توجيه هو ما يحرق التوكنات. وجّه قبل أن تجري.

## 🩺 فحص سريع قبل البحث

```bash
openclaw mcp probe shamela    # يجب أن يعرض 34 أداة — إذا علق: زد المهلات (أعلاه)
```

إذا رجع probe بصفر أدوات أو خطأ → شخّص بالإعدادات (المسار، java) **قبل** أي بحث.
