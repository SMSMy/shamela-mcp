# AGENTS.md — تعليمات إلزامية للأجنت الذي يستخدم سيرفر shamela MCP

> هذا ملف موجّه **للأجنت (AI agent)** الذي يحمّل سيرفر shamela MCP — اقرأه كاملاً قبل أول استخدام. المستخدم البشري لا يحتاج قراءته.
> صالح لكل بيئة: **OpenClaw (WSL2 أو Windows)، DSH، Claude Desktop، أو أي عميل MCP آخر**.

## ما هذا السيرفر؟

مكتبة الشاملة 4 كسيرفر MCP **للاسترجاع فقط** — لا يكتب، لا يحذف، لا يتصل بالإنترنت. 34 أداة بحث وقراءة وإحالة.

**أسماء الأدوات تختلف بادئتها حسب المضيف:**
- OpenClaw / عميل عام: `shamela_search_pages`، `shamela_get_page`...
- DSH: `mcp__shamela__shamela_search_pages`...
- القاعدة: افتح قائمة أدواتك وابحث عن الاسم الذي يحوي `shamela_` — واستخدم ما يعرضه مضيفك فعلاً، لا ما كتب هنا حرفياً.

**الأرقام متغيرة دائماً:** عدد الكتب والمؤلفين والصفحات يختلف من جهاز لآخر، **ويتغير على الجهاز نفسه** — المكتبة تنمو شهرياً (نحو 10 كتب أو أكثر). فلا رقم ثابت صحيح؛ أداة `shamela_health` وحدها تعطيك أرقام اللحظة — لا تنقل أرقاماً من هذا الملف أو من ذاكرتك.

## ⚠️ القاعدة الأولى: لا تخترع شيئاً — استخدم الأدوات مباشرة

- الأدوات **متاحة لك كأدوات MCP مباشرة** — استدعِها كما تستدعي أي أداة أخرى.
- **ممنوع نهائياً**: كتابة سكربتات يدوية، عملاء MCP مخصصين، عمليات خلفية، polling، أو محاكاة البروتوكول يدوياً. هذا أحرق 11 مليون توكن في جلسة سابقة ووصلنا لصفر نتيجة — لا تكرره.
- الأداة الرسمية تجيب خلال **ثوانٍ** (بعد إحماء أول نداء). إذا شعرت أنك «تعيد اختراع العجلة» → توقف فوراً وارجع للأدوات الأصلية.
- الفحص الرسمي أداةٌ لا سكربت: `shamela_health`.

## 🔧 التثبيت والربط — اسأل المستخدم ولا تفترض المسار

**الاكتشاف التلقائي أولاً:** السيرفر يجد بنفسه مجلد الشاملة (سجل النظام على ويندوز + المسارات الشائعة) وجافا المرفقة مع الشاملة. **لا يحتاج أي متغيرات في الحالة الطبيعية** — جرّب أولاً، واسأل المستخدم فقط إن فشل الاكتشاف أو أخطأت الأدوات.

متغيران **اختياريان** للتجاوز عند الحاجة فقط:
- `SHAMELA_INSTALL_ROOT`: المجلد الذي فيه `database/` و `app/` — قد يكون `D:\shamela4` أو `~/Library/Application Support/Shamela` أو أي مكان آخر. **اسأل المستخدم عن المسار الفعلي** إذا لم يكن واضحاً — لا تبحث عنه بنفسك وتحرق وقتاً.
- `SHAMELA_JRE`: مسار جافا تنفيذي (إعداد متقدم) — مثال على WSL2: `/usr/bin/java`.

**أمثلة ربط لكل بيئة:**

OpenClaw (WSL2 أو Windows):
```bash
openclaw mcp set shamela '{
  "command": "node",
  "args": ["<مسار المشروع>/dist/index.js"],
  "env": {"SHAMELA_INSTALL_ROOT": "<مسار الشاملة عند المستخدم>"},
  "cwd": "<مسار المشروع>"
}'
```

DSH — أضف صفاً في ملف الـ preset النشط (`~/.dsh/.agent-presets/<preset>/agent.cordis.yml`):
```yaml
- id: mcp-shamela
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    serverName: shamela
    transport: stdio
    command: node
    args: ['<مسار المشروع>/dist/index.js']
    env:
      SHAMELA_INSTALL_ROOT: <مسار الشاملة عند المستخدم>
```
ثم أعد تشغيل المضيف أو افتح جلسة جديدة (الجلسات الحية تبقى على الجيل القديم).

Claude Desktop / عميل MCP عام: ثبّت ملف `.mcpb` من واجهة العميل، أو سجّل `node <مسار>/dist/index.js` بالأمر المكافئ.

**قاعدة عامة:** إن وُجدت الأدوات في قائمتك فالتثبيت تمّ. إن لم توجد → اسأل المستخدم: «بأي بيئة تشغّل؟ وما مسار مجلد الشاملة؟» ثم طبّق قسم البيئة الصحيح.

## 🕐 الإحماء والمهلات (سبب فشل سابق)

- أول نداء يحتاج **10–15 ثانية** (إقلاع JVM + فهارس Lucene) — البطء الأول طبيعي، لا تعده عطلاً.
- المضيف الذي يدعم مهلات: ارفعها بدل أن تظن السيرفر معطلاً — مثال OpenClaw:
  ```bash
  openclaw mcp configure shamela --connect-timeout 60 --timeout 300
  openclaw mcp reload
  ```
- بعد تغيير الربط: الأدوات قد تظهر في **الجلسات الجديدة فقط** (أو بعد إعادة تشغيل المضيف) — اطلب من المستخدم ذلك. **لا تلجأ لسكربتات بديلة.**

## 🩺 فحص سريع قبل البحث (كل البيئات)

1. استدعِ `shamela_health` — إن عادت أرقام المكتبة وعيّنة قراءة سليمة فكل شيء موصول.
2. إن لم تجد الأداة أصلاً في قائمتك → الربط لم يُفعَّل في جلستك؛ ارجع لقسم التثبيت واسأل المستخدم.
3. إن علق النداء الأول → مهلة قصيرة (قسم الإحماء)، لا تشخيص يدوي.

## 🔍 طريقة البحث الصحيحة (دروس من أخطاء سابقة)

1. **ابدأ بالعبارة الدقيقة**: `shamela_search_phrase(query, mode="phrase")` — تتابع كلمات متتالية.
2. **الرواية تختلف**: نفس البيت/المقولة تأتي بألفاظ مختلفة (ربي/ربّ، قدّر/قدر، همزات، تشكيل). صفر في الحرفي → **جرّب صيغاً أوسع** (جزء مميز فقط، أو كلمات AND على الصفحة).
3. **التمييز بين النتائج**: قد تجمع كلمة بحث نتائج لا علاقة لها بالمراد (استعمالات لغوية، جمل أخرى) — افحص الـ snippets وعدّ الكتب ذات الصلة فعلاً، ولا تخلط تطابقاً زائفاً بنتيجة حقيقية.
4. **اقرأ قبل أن تنسب**: النتيجة → `shamela_get_page` لقراءة الشاهد كاملاً → `shamela_get_citation` للإحالة. لا تنسب نصاً لمصدر لم تقرأه، وميّز المتن عن حاشية المحقق.
5. **اقتباس محور الجواب؟** تحقق منه بـ `shamela_verify_quote` قبل اعتماده.

## 💬 متى تسأل المستخدم (بدل حرق التوكنات)

اسأل **قبل** العمل المكلف إذا كان غير واضح:
- مسار تثبيت الشاملة غير معروف (والاكتشاف التلقائي فشل) → اسأل، لا تبحث.
- بيئة التشغيل غير معروفة عند الحاجة للربط → اسأل: OpenClaw؟ DSH؟ غيرها؟
- العبارة/الموضوع غامض أو محرّف → اذكر الاختلافات المحتملة واسأل أيها يقصد.
- الكتاب في الفهرس لكنه غير منزَّل (`downloaded=false`) → أخبر المستخدم أنه ينزّله من تطبيق الشاملة (أرشده بـ `shamela_suggest_download`)، ولا تحاول القراءة.
- النتائج كثيرة جداً (>10) → اعرض أبرزها واسأل عن الباقي.

**المبدأ:** البحث الواحد عبر الأدوات الأصلية رخيص وسريع — البحث العشوائي المتكرر بدون توجيه هو ما يحرق التوكنات. وجّه قبل أن تجري.

## 📎 قواعد تفصيلية عند التعمق

- في هذا المشروع مجلد `skills/shamela-researcher/` (ملفا `references/search-decision.md` و `references/tools-guide.md`) — اقرأهما كاملين عند أي بحث معمق: قواعد التضييق والتوسيع، بروتوكول صفر النتائج، درجات الثقة، وقواعد النسبة والأمانة.
- في بيئة DSH: مهارة `shamela-researcher` مسجَّلة على مستوى المستخدم وتُحمَّل عبر أداة `skill` متى طابق الطلبُ وصفَها.
